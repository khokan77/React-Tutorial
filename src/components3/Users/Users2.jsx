import React, { use } from 'react';

const Users2 = ({userPromise}) => {
    const users = use(userPromise);
    return (
        <div>
            
        </div>
    );
};

export default Users2;